<h2><%=@member.name%></h2>

<p>Membership status:
  <% if (@member.premium() == true) %>
  Premium
  <% elsif (@member.premium() == false) %>
  Standard
  <% end %>
</p>
<p>Lessons: </p>
<ul>
  <% if (@member.lessons == []) %>
    <p>none</p>
  <% else @member.lessons.each() { |lesson| %>
    <p><%=lesson.course()%></p>
  <% } end %>
</ul>
<hr/>

<form action="/members/<%=@member.id()%>/add" method="post">
  <input type="hidden" name="member_id" value="<%=@member.id%>">
  <select name="lesson_id" required>
    <option value="" disabled selected>Choose new lesson</option>
    <% @lessons.each() { |lesson| unless ((lesson.peak == true && @member.premium == false) || (@member.lesson_ids.include?(lesson.id)))%>
      <option value="<%=lesson.id%>"><%=lesson.course%></option>
    <% end } %>
  </select>
  <input type="submit" value="Add new lesson">
</form>

<form action="/members/<%=@member.id%>/remove" method="post" <% if (@member.lessons == []) %> hidden <%end%>>
  <input type="hidden" name="member_id" value="<%=@member.id%>">
  <select name="lesson_id" required>
    <option value="" disabled selected>Choose lesson to remove</option>
    <% @lessons.each() { |lesson| if (@member.lesson_ids.include?(lesson.id))%>
      <option value="<%=lesson.id%>"><%=lesson.course%></option>
    <% end } %>
  </select>
  <input type="submit" value="Remove a lesson">
</form>

<form action="/members/<%=@member.id()%>/edit" method="get">
  <input type="submit" value="Edit this member">
</form>
<form action="/members/<%=@member.id()%>/delete" method="post">
  <input type="submit" value="Delete this member">
</form>

<p><a href="/members">Return</a> to directory.</p>

<h3><%=@member.name%> now attending <%=@lesson.course%>.</h3>
